// import { initGlobalState } from "qiankun";
// const initState = {
//   // 初始化数据
//   msg: { name: "lihua" }
// };
// const actions = initGlobalState(initState);
// actions.onGlobalStateChange((state, preState) => {
//   // state(主应用变更前),preState(主应用变更后)
//   console.log(state, preState);
// });

// export default actions;
